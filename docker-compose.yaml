services:
  file-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: file-analyzer
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - /mnt/c/Users/taras:/data:ro